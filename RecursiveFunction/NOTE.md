## NOTE

1. What is function?
-> is a block of code to perform specfic tasks


2. What is Recursive Function?
-> simple: a function that call itself to solve a smaller instance of the same problem until it reaches a base case (a condition that stops the recursion)

There are two part of a recursive function:
- base case: case that stop recursion
- recursion case: case that call ifself